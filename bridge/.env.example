EFSS_BRIDGE_HOST=127.0.0.1
EFSS_BRIDGE_PORT=8787

# Optional: stable editor token instead of a random token printed at startup.
# EFSS_EDITOR_TOKEN=replace-with-a-long-random-secret

# Required if you deploy the MCP endpoint directly on a network.
# EFSS_MCP_TOKEN=replace-with-a-different-long-random-secret
